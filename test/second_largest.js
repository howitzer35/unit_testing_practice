var secondLargest = require('../second_largest');
var chai = require('chai');
var expect = chai.expect;

describe('Testing Sums', function() {
  
  context('given variables', function() {
  it('returns the second largest', function() {
      var answer = secondLargest([8,3,5,1,9,10]);
      expect(answer).to.equal();
      });
    });
});